# Conda aliases
# (really, mamba aliases)

alias ma="mamba"
alias maa="mamba activate"
alias mai="mamba install"
alias mac="mamba install -c conda-forge"
alias mrm="mamba env remove -n"
alias mls="mamba env list"
alias mcr="mamba create -n"
alias mad="mamba deactivate"
